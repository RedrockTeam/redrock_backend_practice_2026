# 第一课练习：基础语法与 slice

按目录顺序完成 `01-basics` 和 `02-linear-lookup`。实现 starter 中的 TODO，让公开测试通过。

1. `Greeting`：处理普通名字和空名字；
2. `Average`：处理空 slice；
3. `FindByName`：找不到时返回 `false`；
4. `FormatStudents`：清理名字空白并逐行输出。

最后观察线性查找和重复格式化的维护成本。它是第二课 map/interface 的转场。

```bash
go test ./lesson-01-basics/...
```
