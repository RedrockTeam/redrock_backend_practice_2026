# 题目设计与课堂转场

每个 `testNN/README.md` 是学生看到的任务边界，不含答案；公开测试就是行为契约，CI 只按测试终态给分。这份文档写给出题人，说明每道题为什么在这个位置。

## 第一课：基础（6 题）

| 题目 | 包 | 对应课件 | 要点 |
|---|---|---|---|
| `test01` | `hellogo` | 第二部分 | 第一个能跑的程序，认识 `package main` 和 `func main` |
| `test02` | `syntax` | 第三部分 | 变量、条件、循环、`fmt.Sprintf`；顺带看到同包测试能调用小写函数 |
| `test03` | `packagerules` | 第四部分 | 不写业务代码，改一棵故意违规的 `testdata` 目录树：包名与目录名不符、导入 `main`、跨模块引用 `internal/`、包循环 |
| `test04` | `visibility` | 第五部分 | 大小写即可见性；代码拆成两个文件，证明「包内可见，不是文件内可见」；外部测试用反射挡住「把字段改成大写」的绕过 |
| `test05` | `even` | 第五部分 5.6 | 给定一个有 bug 的 `IsEven`，学生自己写测试把 bug 暴露出来；契约测试检查他们确实覆盖了 0 和负数 |
| `test06` | `roster` | 衔接题 | 见下 |

### test06 是整课的出口

前五题都在第一课的知识范围内。`test06` 故意多要一点：学生要用**切片**存名册，用**多返回值** `(Student, bool)` 表达「查到了吗」。这两样第一课没讲，README 就地补——不是打补丁，而是因为后面正好都要用。

题目本身很朴素：往名册里加学生，按 ID 查人，按姓名统计总分，再把名册格式化成两种文本。关键在 `Roster` 里埋了一个 `comparisons` 计数器，于是三道 `TestBridge...` 能**量出**代价而不只是描述它：

- `TestBridgeLookupCostGrowsWithRosterSize`：200 条名册，命中首条 1 次比较，命中末条 200 次，查不到 200 次；
- `TestBridgeAggregationScansWholeRoster`：每问一次「这个名字总分多少」就重扫 200 条，问两次 400 次——切片没有任何记忆；
- `TestBridgeTwoFormattersShareOneSignature`：两个格式化函数被塞进同一个 `[]func(*Roster) string` 里依次调用，说明它们已经能被统一调用了。

这三个测试在做对时**是通过的**，痛感来自被断言出来的具体数字和 `t.Log` 的旁白，而不是人为制造的失败。README 末尾把它们直接兑换成下一课：

| 这一题遇到的 | 第二课的答案 |
|---|---|
| 查一个人要扫 N 条 | `map[string]Student`，`student, ok := m[id]` 和刚写的 `(Student, bool)` 同一个形状 |
| 每次统计都重扫一遍 | `map[string]int`，一次遍历攒好所有名字的总分 |
| 两个格式化函数重复同一个循环 | `interface`，遍历写一次，每行长什么样交给不同实现 |

顺带也把 `FormatText` / `FormatCSV` 写成了自由函数而不是方法——它们签名完全相同，离 interface 只差一步。

## 第二课：集合

`test01` 用 map 统计多次成绩，`test02` 让两个 formatter 落到同一个 interface 上（正好是 test06 留下的那个形状）。`test03` 故意共享一个 map，普通运行可能「碰巧通过」，`go test -race` 会报数据竞争，严重时直接 `fatal error: concurrent map writes` 打死进程。评分脚本对这种没有终止事件的崩溃有专门处理：它会归因到触发的那个测试函数，而不是笼统记成编译失败。

## 第三课：goroutine

`test01` 先保持顺序，`test02` 比较顺序与并发的耗时，`test03` 启动 goroutine 却立即返回，测试稳定地暴露结果缺失。第二课埋的竞态在这里得到解释：共享内存是什么、为什么会乱。

## 第四课：同步

`test01` 合并多个 channel，`test02` 修一个并发计数器，`test03` 实现固定 worker 数量的任务池。结课题同时要过功能测试和 `-race`，四课的概念在这里连成一条完整的数据流。

## 出题时的几条约束

- 题目单位固定为 `testNN/`，Go 包放在它下一层，包名与目录名一致；
- 每个 `testNN/` 必须有 README，说明做什么、对应课件哪一节、评分点是哪几个测试函数；
- starter 必须能编译——学生该看到的是测试失败，不是一屏语法错误。用 `return fmt.Sprintf("")` 这类「能过编译但显然不对」的占位，别用 `_ = fmt.Sprintf`；
- 公开测试的函数名就是评分点，改名等于改评分接口；
- 需要故意留 bug 的题（如 `test05`），配一个契约测试检查学生的测试确实覆盖了边界，否则「写一个恒真的测试」也能拿分。
