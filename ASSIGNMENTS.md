# 题目设计与课堂转场

这些 README 是学生看到的任务边界，不包含答案。每题的公开测试就是行为契约，CI 只根据测试终态给分。

## 第一课：基础

先做问候、平均分等小函数，再实现 slice 线性查找。最后一题要求增加一种输出字段，学生会看到重复遍历和拼接越来越难维护；第二课将其改成 map 和 interface。

## 第二课：集合

先统计 map 中的多次成绩，再实现文本/JSON formatter。最后的 `03-map-race` 故意共享 map，普通运行可能“碰巧通过”，`go test -race` 会报告数据竞争；第三课先解释 goroutine 与共享内存。

## 第三课：goroutine

先保持顺序的任务函数，再比较顺序和并发的计算。最后的 `03-missing-results` 启动 goroutine 却立即返回，测试稳定地暴露结果缺失；第四课使用 channel 和 WaitGroup。

## 第四课：同步

先合并多个 channel，再修复并发计数器，最后实现固定 worker 数量的任务池。结课题同时通过功能测试和 `-race` 反馈，学生能把四课概念连成一条数据流。
